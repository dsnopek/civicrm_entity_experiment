{#
/**
 * @file civicrm_event.html.twig
 * Default theme implementation to present CiviCRM Event data.
 *
 * This template is used when viewing CiviCRM Event pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_civicrm_event()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('civicrm_event') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
